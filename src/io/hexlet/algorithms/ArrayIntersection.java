package io.hexlet.algorithms;

public class ArrayIntersection {
    public static int[] getIntersection(int[] numbers1, int[] numbers2) {
        int i,j = 0;
        int[] result = new int[Math.min(numbers1.length,numbers2.length)];
        int index = 0;

        while (i < numbers1.length && j < numbers2.length) {
            if()
        }
    }
}
